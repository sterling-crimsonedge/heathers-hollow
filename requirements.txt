# Heather's Hollow AI server.
#
# Villager conversations go through the `claude` CLI (Claude Code) rather
# than the Anthropic API directly — see README "Running the demo". That
# means we use Sterling's Claude subscription, not metered API calls, and
# we don't need the `anthropic` SDK at all.
#
# Make sure the `claude` CLI is installed and on your PATH:
#   https://claude.com/claude-code

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
websockets>=12.0
